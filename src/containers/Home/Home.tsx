import React from 'react';
import AppContainer from '../AppLayout/AppContainer';

const Home: React.FC = () => {
  return <AppContainer title={('Chúc bạn một ngày tốt lành!')} />;
};

export default Home;
